# 更新日志

## 1.2.0-beta.1（预览版）

> 这是一个**预览版**（npm 上的 `beta` 标签），装着还没正式发布的一批改动。
> 正式版仍请用 `latest`（当前 1.1.0）。预览版建议先在小范围群里试，遇到问题把日志发我。

### 本版重点
- **在线播放器**（通用 →「在线播放器设置」）：选「弹幕」时不再把弹幕烧进视频，而是把视频放到一个播放页，
  给用户一条链接；播放页照着 B站播放页做（夜间风格、标题/UP/统计、弹幕设置面板），
  支持弹幕开关/字号/透明度/显示区域/滚动·顶部·底部三类开关/彩色弹幕，链接到期自动删文件。
- **「弹幕」与「在线看」按钮合并**（播放器开着时两者是同一个动作），表格后的说明不再被当成表格行。
- **解析流程统一成「先下载视频、再渲染卡片」，单步失败不再中断**（抖音 / B站 / 小红书 / 快手），
  失败的步骤会在最后合成**一张**错误卡片。
- **进度阶段补齐**：正在获取下载链接 → （正在合并音轨）→ 正在烧录 / 正在准备在线播放 → 正在发送。
- 面板新增「打开原站」链接；播放页与过期页新增「直接跳转 / 手动复制」；播放页可下载视频。
- 修复：**av 号链接解析失败**（av→BV 转换返回结构多包一层导致 bvid 取不到、静默失败）；
  QQ 面板链接用 `mqqapi://` 被服务端拒绝导致整条面板发不出去；错误卡片切片 `e is not defined`；
  `getFriendList` 刷屏；浏览器禁访端口（ERR_UNSAFE_PORT）自动退回。


### 新增
- **在线播放器（弹幕在线看）**（通用 →「在线播放器设置」，总开关**默认开启**）：
  解析面板里那一列按钮写「**弹幕**」，点它不再把弹幕烧进视频，而是把视频下下来放进播放器目录、
  回一条链接 —— 点开就是带弹幕的播放页（深色界面、手机也能看，弹幕用 canvas 自己画，
  有弹幕开关 / 字号 / 透明度三个控件，进度条可拖动）。页面不依赖任何外网 CDN，服务端支持 HTTP Range。
  - 配置项：**在线播放器**（默认开启）、**播放器公网地址**（默认空，留空会退化成「本机 IP + 端口」
    并在日志里提醒，本机 / 内网照样能看）、**播放器端口**（默认 0 = 复用 Koishi 自己的端口）、
    **链接有效期（分钟）**（默认 60，最小 1、最大 1440，到点自动删视频和弹幕，链接变「链接已过期」）、
    **在线播放最大文件（MB）**（默认 0 = 跟随全局的「文件大小限制」，超过就不走在线播放、改回原来的发送流程）、
    **超限转在线播放**（默认关：打开后，超过全局「文件大小限制」的视频不再回「视频太大了，还是去B站看吧」，
    而是照常下载并改成在线播放 —— 用户拿到播放链接，视频不发群、也不占群文件）。
  - **只有总开关**「弹幕重定向在线播放器」和通用里的「强制不烧录弹幕」联动：
    关掉「强制不烧录弹幕」（= 打开弹幕功能）之后才能改它，其余四项随时可编辑。
  - 关掉总开关就回到原来的 ffmpeg 烧录流程（列头也从「弹幕」变回「烧录弹幕」）。
  - 路由：`/kkk/player/:token`（播放页）、`/kkk/player/:token/video`（支持 Range）、
    `/kkk/player/:token/danmaku`（弹幕 JSON）；令牌是随机串且只允许 `[0-9a-z]{8,64}`。
  - 播放器出问题不会连累解析：登记失败会回一句说明并退回「直接发视频文件」。
- **通用 → 强制不烧录弹幕**（默认开启）：弹幕烧录的总开关，优先级最高。
  开着时不管是指令、面板按钮还是平台配置，都不会烧录弹幕，一律按纯视频解析；要用弹幕功能就先把它关掉。
- **QQ 适配器 → 面板带「打开原站」链接**（默认开启）：解析面板（以及番剧选集面板）下方多一个
  「打开原站」按钮，点一下直接跳到抖音 / B站 的原作品页。
- **QQ 适配器 → 面板显示「烧录弹幕」列**（默认关闭）：打开后，QQ 里的解析面板会从
  「清晰度 | 大小」两列变成「清晰度 | 烧录弹幕 | 大小」三列 —— 用户点中间那列就按对应画质
  带弹幕解析，左边的画质按钮仍是纯视频。面板里没有额外的开关按钮，显示与否只看这一项配置。

### 修复
- **在线播放页按B站播放页重做**（用户要求「参考 B站 UI」）：
  深灰底 + 白字的夜间风格；顶部封面小卡（带时长角标）+ 大字标题（最多两行）+ 一行小图标统计
  （播放量 / 弹幕数 / 发布时间 / UP 主）；视频下方是弹幕控制条（**样式化开关** + 「弹幕设置」按钮），
  设置面板默认收起，点开后有**字号 / 透明度 / 显示区域**三行；最下面是B站那排操作按钮
  （点赞 / 投币 / 收藏 / 评论 / 分享，**只用真实数据，没有就不显示，不编数字**）。
  移动端控件不挤：字号 / 显示区域都是大号分段按钮，统计行自动换行。
  仍然是**零外网依赖**（无 CDN、无第三方库，图标全是内联 SVG），弹幕仍是 canvas 自绘。
- **播放页会带上作品信息**：解析时顺手把标题 / UP 主 / 封面 / 播放量 / 点赞 / 投币 / 收藏 /
  评论 / 分享 / 发布时间 / 时长存进播放会话（B站取 `infoData.data.data`，抖音取 `aweme_detail`），
  页面上按B站那样展示；封面在登记会话时下到本地、走**同源** `/kkk/player/<token>/cover`，
  所以断网 / 内网也不会裂图；拿不到的字段就不显示。
- **在线播放优先选 H.264 的流**（用户实测「返回的视频只有声音」）：B站同一个清晰度会同时给出
  `avc1` 和 `hev1` 两路，接口常把 HEVC 排在前面，浏览器（没装 HEVC 扩展的 Chrome / Edge）
  只能放声音。现在在线播放模式选流时优先挑 H.264（自动档位是去重之前把 H.264 排到前面），
  正常发视频的行为不变；页面上也补了提示：真遇到解不了的编码会明说「只有声音没有画面」。
- **播放器链接打不开、浏览器报 `ERR_UNSAFE_PORT`**（用户实测把端口配成了 6666，属于 IRC 段）：
  服务端一切正常，是浏览器按黑名单**禁止访问**这类端口。现在：
  - 启动时与生成链接时都会打一条明确警告（换端口，或者配公网地址走反向代理）；
  - **端口在黑名单里、又没配「播放器公网地址」时自动退回 Koishi 自己的端口**（链接照常能用，
    不用先去改配置）；配了公网地址的反向代理场景照旧用你配的端口；
  - 「播放器端口」的字段说明里写明了别用 6665-6669 这类端口。
- **打开了在线播放器，面板里却没有「弹幕」按钮**（用户实测）：那一列当时还被 QQ 适配器里的
  「面板显示「烧录弹幕」列」（默认关）管着，等于要开两个开关才有入口。
  现在在线播放模式下这一列**只跟「弹幕重定向在线播放器」走**（开着就有），
  烧录模式下才由那个旧开关决定；旧开关的说明也同步写清楚了。
- **在线播放器设置整组被锁住**（用户实测）：上一轮加的「需先开启弹幕功能才能编辑」判断**写反了** ——
  实际效果是「开着『强制不烧录弹幕』（= 弹幕功能关着）时能编辑，关掉之后反而整组变灰」，
  而且把 5 个字段全绑上了。现在改成：**只有总开关**（顺带改名为「弹幕重定向在线播放器」）
  受「强制不烧录弹幕」联动，方向已修正（关掉它之后才能改总开关），
  其余四项（公网地址 / 端口 / 有效期 / 最大文件）随时可编辑。
- **「打开原站」链接把整条面板拖挂了**（严重）：链接原来写成 `mqqapi://forward/url?...`，
  QQ 官方接口直接拒收（`[40034028] 请求参数不允许包含url mqqapi://forward/url`），
  **整条面板消息都发不出去** —— 面板失败 → 解析失败 → 最后只剩一张错误卡片。
  现在改成普通 markdown 链接 `[打开原站](https://…)`（同一条消息里的卡片图用的就是 https 链接，
  说明这种形式是被接受的），并加了兜底：面板带链接发送失败时**自动去掉链接行重发一次**
  （解析面板、番剧面板都走这条兜底），链接发不出去再也不会连累解析。
- **错误卡片切片失败 `e is not defined`**：`sliceImageToMarkdown` 里混进来一段从
  `sendSlicedImage` 复制过来的「非 QQ 平台」分支，用了这个函数根本不存在的 `e` / `valid` ——
  函数一调用就 ReferenceError，错误卡片永远切不了片，最后那张 8.9MB 的长图直接原样发出去
  （还可能被 QQ 拒收）。该分支已删除，切片恢复正常。
- **`getFriendList 失败: TypeError` 刷日志**：兼容层直接调了适配器不一定实现的方法
  （qq-crack 就没有）。改成先做能力判断，没有这个方法就安静返回空数组。
- **「下载进度详情」里看不到解析阶段了**：现在 B站 / 抖音 / 快手 / 小红书 四条链路都能看到
  「正在获取下载链接 → 正在合并音轨（有的话）→ 正在烧录 / 正在准备在线播放 → 正在发送」。
  之前只有抖音 / 快手 / 小红书登记过「正在获取下载链接」（B站一个阶段都没有），
  「正在烧录」「正在发送」这两个状态则从来没实现过；解析流程改成「先下载、再渲染、最后上传」之后，
  中间几段更是完全没有登记点，用户点「下载进度」只会看到「当前没有正在进行的下载」。
  - 阶段挂在**解析共用的一个 key** 上：后一个阶段覆盖前一个，同一次解析永远只有一条，
    不会出现「正在烧录」和「正在发送」并排打架；
  - 登记点集中在共用出口：解析入口（`beginParseStage`）、音视频合成（`mergeVideoAudio`）、
    烧录（`withDownloadStage` 包住 B站 / 抖音的烧录调用）、上传（`uploadFile`）、
    在线播放（`publishOnlinePlayer`）；
  - 每个阶段结束都会清理（成功失败都清），解析结束还会在命令入口兜底清一遍，
    不会再卡在「正在获取下载链接」；顺带修掉了实况合成带 BGM 那条分支漏清理的问题。
- **弹幕烧录一直不生效**：上一轮「移除弹幕功能」时把烧录请求写死成了 false，
  指令 `弹幕解析`、参数 `--dm=1` 和平台配置里的 `burnDanmaku` 都不会真的烧弹幕，现在都恢复，
  并且统一过一遍上面那个总开关（不存在「面板关了、指令还能烧」这种半开状态）。
- 「弹幕解析」指令重新注册（控制台指令列表里能看到），指令说明同步改成用户能看懂的说法。
- 面板在「所有画质都超过体积上限」时补回提示：「这里只保留最小的一档，发送可能失败」。

### 变更
- **解析流程改成「先下载视频，再渲染卡片」，并且单步失败不再中断整个解析**（抖音 / B站 / 小红书 / 快手）：
  - 选完清晰度后先下载（需要烧录的连合成、烧录一起做完），再去渲染信息卡、评论区；
  - 中间任何一步失败都只跳过这一步、继续跑后面的，能发的视频/卡片照发；
  - 全部跑完后如果确实有失败，才渲染**一张**错误卡片（里面会列出是哪几步失败了），
    错误不再出现在「视频明明能发、却因为一张卡片渲染失败而什么都没有」的场景里。

## 1.1.0

### 变更
- **插件配置只在 WebUI 面板里改**：控制台插件配置页里的设置项已全部隐藏，新字段也会自动进面板对应分类，
  用户看一眼就知道去哪儿设置，不会再出现「控制台里改了一半、面板里改了另一半」的情况。
- **扫码登录权限 / 错误日志接收人支持手填**：可以直接写 `*`（所有人）或账号（多个用逗号分隔），
  错误日志也能私聊发给指定账号；界面上是下拉选模式，只有选「指定账号」时才出现输入框。
- 权限写法统一成 Koishi 的说法：`all` / `admin`（权限等级 4 及以上）/ 指定账号，不再出现 Karin 的 master / allMasters。
- 「自动撤回面板」开关挪到面板的「通用」分类（原来在 QQ 适配器里，不容易找）。
- 插件说明（控制台里的用法提示）重写为面向用户的说明：配置入口、指令表、QQ 用法。
- **API 服务默认关闭**（`APIServer` / `APIServerMount` 默认 false）：不需要的人不用被迫开一个 HTTP 端口。

### 修复
- **QQ 群文件阈值在面板里改了不生效**：判定处读的是上游 config.json 的 Proxy，取不到的键会返回 `{}`，
  `Number({})` 是 `NaN`，阈值判断恒为假（日志表现为 `QQ阈值=NaNMB`）。现在改读 Koishi 侧的运行时配置，
  超阈值视频会正常走群文件发送。
- QQ 平台判定取不到平台名（只读 `event.platform`，实际在 `event.bot.bot.platform`），导致群文件分支走不到。
- 面板里的下拉框改用面板自带的 Select 组件（此前用的是浏览器原生下拉，和面板样式不一致）。

## 1.0.9

### 变更
- **面板不再从独立 URL 进**：`/kkk`、`/kkk/edit` 以及 `/kkk/assets/*` 在未授权时直接返回 404
  （以前是一个「需要先登录」的提示页）。面板只从控制台侧边栏「kkk 配置」进，
  凭据由控制台页面通过 RPC 换取的面板 token 交付。
- **QQ 平台单独的视频体积阈值**：新增配置项 `qqGroupFileLimitMB`（QQ 适配器分组，默认 30）——
  视频超过该体积时改用「群文件」发送，不再受上游 `usegroupfile` 开关影响；
  其它平台仍按上游的「群文件上传」+ `groupfilevalue` 判断。

### 修复
- 之前 30MB~100MB 的视频没有按群文件发送：运行时配置里 `groupfilevalue` 是 100（不是 30），
  这一区间会走视频通道，被 QQ 压缩/改名。现在 QQ 用上面的独立阈值判断。

## 1.0.8

### 修复
- 控制台入口包加载失败导致**侧边栏「kkk 配置」整个消失**：上一版加 RPC 失败日志时把 `try / else` 括错了，
  注册面板路由的那段代码跑到作用域外（`ReferenceError: server is not defined`），现在结构修正。
- 面板前端包禁用缓存（`/@plugin-<id>/index.js` 加 `no-store`）：控制台默认不带缓存头，
  浏览器会一直用旧 bundle，表现为「代码改了但界面没变」。

### 变更
- 控制台页面取登录凭据改成两条路依次尝试（RPC → store.user），都不行时**把失败原因显示在页面上**，
  方便一眼看出是没登录还是别的问题。

## 1.0.7

### 修复
- **面板报「加载配置失败 / 鉴权失败: 缺少authorization」**：面板自己的登录 token 存在服务端内存里，
  Koishi 一重启就失效，而自动登录脚本看到 localStorage 里已经有 token 就跳过登录，
  于是一直拿着废 token 请求、所有接口 401。现在：
  - 每次打开面板都会重新换一个新的 token（不再因为 localStorage 有值就跳过）；
  - 接口鉴权同时接受「控制台换来的面板 cookie」，即使前端 token 过期也能正常读写配置。
- 1.0.6 引入的登录校验配合这次修复才算完整：现在（装了 auth 插件时）未登录打不开面板、
  登录后配置能正常加载与保存。

## 1.0.6

### 变更
- **配置面板默认要求登录**（`webUiAuth` 默认开）：装了 auth 插件的部署，未登录时打开 `/kkk` 会直接看到
  「需要先登录 Koishi 控制台」，静态资源与接口同样不放行。做法和控制台页面一致 ——
  控制台里的「kkk 配置」页通过带权限校验的 RPC 向服务端换取一次性面板 token，再拼进 iframe 地址；
  拿不到 token（未登录/权限不足）就不加载面板。没装 auth 插件的部署不受影响（本来也没有登录这回事）。
- 面板页面不再需要「HTTP 鉴权密钥」：能进来就说明已经授权，登录态由服务端自动补齐。

## 1.0.5

### 修复
- 「关于插件」页的大标题漏改，仍显示 `karin-plugin-kkk`，现在改为 `koishi-plugin-kkk`。

## 1.0.4

### 修复
- 1.0.3 里注入版本号那段把中间件里的 `const` 又赋了一次值，导致 `/kkk/assets/*.js`（主题/布局那几个包）返回 500、面板打不开；
  现在改成用新变量承接，已回归正常。

## 1.0.3

### 变更
- 「添加推送目标」里的**推送群改为手填群号**：原版是从 `/kkk/v1/bots/:id/groups` 拉下拉框，
  拿不到群列表时根本选不了（只显示「请先选择机器人账号。」）。现在直接输入群号（频道 id）即可，
  需要指定平台时写「平台:群号」。提交逻辑同步兼容手填，不再依赖选中列表里的群。
- 「关于插件」页更新：标题改为 koishi-plugin-kkk，版本号改成运行时注入的真实版本，
  仓库 / 反馈 / 头像链接指向 maimai993/koishi-plugin-kkk，开发者信息标注为 Koishi 移植版。
- 机器人账号那一项的说明已由「Karin 当前已注册且在线的 Bot。」改为「Koishi ...」（1.0.2 起生效）。

## 1.0.2

### 变更
- 配置面板去掉了写死的用户信息块（原来侧栏一直显示「炫炫 / Super Admin / 当前身份：管理员」）。
- 面板内的 Karin 字样全部改成 Koishi（登录页「Karin 鉴权」、挂载到 Karin、发送方式说明等），
  「帮助文档」按钮改为指向本项目文档站。
- `config/default_config/*.yaml` 里描述字段的 Karin 字样同步改成 Koishi。

### 新增
- `webUiAuth` 打开后，面板页面与静态资源同样要求登录 Koishi 控制台（未登录时连 `/kkk/assets/*` 也不放行）；
  默认关闭，行为与之前一致（免登录）。

## 1.0.1

### 修复
- 生产环境下 `/kkk` 配置面板报「assets/web/index.html 缺失」：插件根目录不再只按 `lib/` 的位置推断，
  改为从入口所在目录逐级向上找带 `assets/web/index.html` 的那一层（并兜底按包名解析、cwd / cwd/node_modules），
  启动日志里会打印解析结果，找不到时会给出警告。
- 配置面板不再强制登录：生产环境装了 auth 插件时，面板原本要求先登录 Koishi 控制台，现在默认免登录；
  需要恢复登录校验时把配置项 `webUiAuth` 打开即可。

## 1.0.0

首个公开版本：**karin-plugin-kkk 的 Koishi 移植版**，把上游的解析 / 推送 / 面板能力完整搬到 Koishi 生态。

### 链接解析与推送
- **B站**：视频、番剧（先选分集 → 再选画质）、动态、直播、专栏、评论
- **抖音**：作品、图集、直播、评论区
- **快手 / 小红书**：作品解析
- 解析内容（信息卡片 / 视频 / 评论）与画质、体积上限、是否带弹幕均可配置，QQ 面板上还能临时选
- 动态推送：B站 / 抖音关注列表更新推送，推送目标、过滤规则、强制推送均可配置
- 卡片解析：群里转发的分享卡片没有链接时，识别封面文字再搜索定位作品（识别密钥 `qq.ocrApiKey`）

### QQ 官方机器人交互面板
- 普通视频：先发一张**解析结果同款卡片图**（带热门弹幕），下面是「清晰度 / 视频 / 弹幕 / 大小」四列表格
- 番剧：卡片图（不再把分集画进图里）+ 集数表格（默认 5×4、倒序排列），表格下方是「上一页 / 第 x/y 页 / 下一页」
- 图片经宿主 `assets` 上传成 https 地址，按 QQ 要求写成 `![#宽px #高px](url)`
- 超过体积上限（默认 200MB）的画质自动隐藏 —— 点了也发不出去
- 面板按钮**无状态**：宿主重启后旧面板依然可用
- **下载进度反馈**：`收到请求，开始下载` 消息带「查询下载进度」按钮，只看本次任务的进度；发送前提示 `发送中…`（走群文件时会提前说明）
- **自动撤回**：解析流程中每发一条新消息就撤回上一条，群里只留最新状态（`qq.recallPanel`）

### 图片发送（QQ）
- 评论区那种超长图默认按普通图片发；只有**超过 20MB** 或**发送失败（拿不到消息 ID / 返回错误码）**时才自动切片
- 切片前缩到 1440px 宽，按 `qq.sliceImageHeight`（默认 2000px）等分，最后用一条 markdown 把切片拼回一整张，视觉上仍是一张图
- 切片与合并只对 QQ 生效，其它平台一律按普通图片发送

### 弹幕烧录
- B站弹幕烧录进视频（`弹幕解析` 指令，或面板上点「弹幕」那一列）
- 编码器自动探测与回退（NVENC / QSV / AMF / libx264 / libx265）
- 低码率源改用 CRF 保证弹幕文字清晰；横竖屏与旋转元数据识别

### 配置管理
- **`/kkk`**：插件自带的一体化配置面板，免登录，保存即写回 `koishi.yml` 并热重载
  - 原版分类：接口库 / 通用 / 抖音 / 哔哩哔哩 / 快手 / 小红书 / 推送列表
  - 另有 **「QQ 适配器」**分类：面板、自动撤回、画质档体积、番剧选集表格、图片切片、卡片识别密钥
- **`/kkk/edit`**：轻量配置页，适合直接改 JSON
- **Koishi 控制台**：侧边栏入口「kkk 配置」打开的就是 `/kkk`；插件配置表单同样分成
  「QQ 适配器 / Koishi 原生设置 / upstream」三组（原生项默认折叠），与控制台表单共用同一份字段定义（`src/qqFields.json`）
- 配置面板自动登录：直接调用登录接口写入登录态，打开即用，不用手点登录框

### 错误诊断
- 错误诊断卡片：框架版本 / 插件版本 / 适配器、堆栈与运行环境，方便直接贴群里问
- 错误通知接收人可配：触发者 / 管理员（权限等级 > 4）
- `kkk版本` 指令输出一张运行环境卡片

### 卡片与模板适配
- 框架版本显示 **Koishi 版本**，页脚换用 Koishi logo，支持作者署名
- 与 Koishi puppeteer 插件配合：大卡片不再因单次渲染占用数 GB 内存而 OOM；四角与底部不再有空白
- 评论区默认开启解析，评论图片合并进评论卡片（不再单独合并转发）

---

## 移植说明

上游 [karin-plugin-kkk](https://github.com/ikenxuan/karin-plugin-kkk) 的完整功能清单请见其仓库。本仓库保留其业务逻辑，由 `src/compat/` 提供框架等价实现，配置以 `koishi.yml` 为准并在启动时同步进上游的 `config.json`。