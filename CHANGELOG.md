# 更新日志

## 1.0.0

首个公开版本：**karin-plugin-kkk 的 Koishi 移植版**，把上游的解析 / 推送 / 面板能力完整搬到 Koishi 生态。

### 新增
- **QQ 官方机器人交互面板**
  - 普通视频：先发一张**解析结果同款卡片图**（带热门弹幕），下面是「清晰度 / 视频 / 弹幕 / 大小」四列表格
  - 番剧：卡片图（不再把分集画进图里）+ 集数表格（默认 5×4、倒序排列），表格下方是「上一页 / 第 x/y 页 / 下一页」
  - 图片经宿主 `assets` 上传成 https 地址，按 QQ 要求写成 `![#宽px #高px](url)`
  - 超过体积上限的画质自动隐藏
  - 面板按钮**无状态**（重启后旧面板依然可用）
- **下载进度反馈**：`收到请求，开始下载` 消息带「查询下载进度」按钮，只看本次任务的进度；发送前提示 `发送中…`（走群文件时提示将以群文件发送）
- **自动撤回**：解析流程中每发一条新消息就撤回上一条，群里只留最新状态（配置项 `recallPanel`）
- **配置管理面板**
  - `/kkk`：照搬原版的 React 配置面板，登录沿用 Koishi 的 auth 插件（没装 auth 即免登录）
  - `/kkk/edit`：轻量配置页，保存直接写回 `koishi.yml` 并热重载
  - Koishi 控制台注册入口页面「kkk 配置」
- **弹幕烧录增强**：编码器自动探测与回退、低码率源改用 CRF 保证弹幕文字清晰、横竖屏与旋转元数据识别
- **评论区**：默认开启解析，评论图片不再单独合并转发（评论卡片里已包含）
- **错误通知接收人可配**：触发者 / 管理员（权限等级 > 4）
- 卡片适配：框架版本显示 Koishi 版本、页脚换用 Koishi logo、支持作者署名

### 修复
- 卡片渲染：修正与 Koishi puppeteer 插件的兼容问题，大卡片不再因单次渲染占用数 GB 内存而 OOM；四角不再露出白色底、底部不再有空白
- 头像 / 装扮图：还原被第三方图床代理包裹的地址（代理在部分网络下不可用，表现为裂图）
- 版本号：卡片页脚不再显示 `koishi-compat` 这类占位值
- 撤回：修正缺少频道号导致的 `recallMsg` 报错
- 番剧面板的翻页按钮改用解析器能识别的 `bangumi/play/ss…` 链接
- 网络：默认 IPv4 优先，规避部分环境下到 B站的 IPv6 连接被重置（`ECONNRESET`）
- 媒体发送：兼容 `base64://` 资源写法，避免适配器把它当 URL 去 fetch 而发送失败
- 模板渲染：缺少分集数据时不再整张卡片退化成兜底卡片

---

## 移植说明

上游 [karin-plugin-kkk](https://github.com/ikenxuan/karin-plugin-kkk) 的完整功能清单请见其仓库。本仓库保留其业务逻辑，由 `src/compat/` 提供框架等价实现，配置以 `koishi.yml` 为准并在启动时同步进上游的 `config.json`。
